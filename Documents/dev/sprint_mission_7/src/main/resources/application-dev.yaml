server:
  port: 8081
  servlet:
    context-path: /dev

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/discodeit
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    root: DEBUG